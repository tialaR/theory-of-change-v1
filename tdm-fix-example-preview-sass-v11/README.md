# TDM example preview Sass fix v11

Corrige a sintaxe `.sass` do componente `example-previews.module.sass`, juntando listas de `background` e `box-shadow` em uma linha para evitar erros do Sass/Turbopack.

Não altera lógica, rotas, `/canvas` ou componentes.
