# SharkOps Architecture

The Installer installs a local, dependency-free Node.js CLI under `tools/sharkops`, project configuration under `.sharkops`, and durable knowledge under `docs/sharkops`.

The engine is reusable. The `.sharkops` directory is the current project's personality and state.
