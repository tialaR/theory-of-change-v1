# Ataque de Tubarão, Onda 01

Esta onda saneia o projeto existente. Não cria outro projeto e não reconstrói a interface.

## Entregas

- remove arquivos, rotas e implementações comprovadamente mortas;
- preserva `/canvas-v4` e a Preview V2;
- cria `/canvas-legado` para o motor anterior;
- direciona `/canvas` temporariamente para a experiência homologada;
- zera o TypeScript e exige build de produção;
- abre uma janela de migração registrada para as três ondas do Canvas;
- mantém merge bloqueado até o lockdown da Onda 03.

## Estado do gate

`open`, controlado pela branch `refactor/shark-cleanup-and-canvas-v1`.
